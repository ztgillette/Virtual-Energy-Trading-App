export const is3DChartType = type => "bar3d" === type || "funnel3d" === type || "histogram3d" === type || "pie3d" === type || "rangeColumn3d" === type || "wordCloud3d" === type;

export const is3DAxisChart = spec => spec.zField || spec.series && spec.series.some((s => s.zField));

export const is3DChart = spec => {
    return !!spec && ("bar3d" === (type = spec.type) || "funnel3d" === type || "histogram3d" === type || "pie3d" === type || "rangeColumn3d" === type || "wordCloud3d" === type || is3DAxisChart(spec), 
    !0);
    var type;
};
//# sourceMappingURL=util.js.map
import { ElementSelect, Factory } from "@visactor/vchart";

const type = "element-select-by-graphic-name";

export class ElementSelectByGraphicName extends ElementSelect {
    constructor() {
        super(...arguments), this.type = type;
    }
    start(markGraphic) {
        const name = null == markGraphic ? void 0 : markGraphic.name;
        name && this.getMarks().forEach((mark => {
            var _a;
            null === (_a = mark.getGraphics()) || void 0 === _a || _a.forEach((g => {
                g.name === name && super.start(g);
            }));
        }));
    }
}

ElementSelectByGraphicName.type = type;

export const registerElementSelectByGraphicName = () => {
    Factory.registerInteractionTrigger(ElementSelectByGraphicName.type, ElementSelectByGraphicName);
};
//# sourceMappingURL=element-select-by-graphic-name.js.map

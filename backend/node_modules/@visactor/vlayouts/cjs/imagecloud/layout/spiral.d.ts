import type { ImageCollageType } from '../interface';
import { Layout } from './basic';
export declare class SpiralLayout extends Layout {
    preProcess(): ImageCollageType[];
    private tryPlaceImage;
    doLayout(images: ImageCollageType[]): ImageCollageType[];
}
